<h1>Edit {{$model}}</h1>
<form action="{{ route('{{resource}}.update', ${{variable}}) }}" method="POST">
    @csrf
    @method('PUT')
        <div>
            {{inputs}}
        </div>
    <button type="submit">Update</button>
</form>